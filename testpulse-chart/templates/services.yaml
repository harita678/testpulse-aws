{{ range .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  selector:
    app: {{ .app }}
  ports:
    - port: {{ .port }}
      targetPort: {{ .port }}
  type: ClusterIP

---
{{ end }}